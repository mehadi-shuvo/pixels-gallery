const nextConfig = {
  images: {
    domains: [
      "localhost",
      "res.cloudinary.com",
      "example.com",
      "https://pixels-server-one.vercel.app",
    ], // Add your image host domains here
  },
};

module.exports = nextConfig;
